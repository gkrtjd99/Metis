# PRD — [목표 제목]

> **TEMPLATE NOT COMPLETE** — 이 문서는 초안 양식이다. 대괄호 필드와 `미정`을
> 사실·사용자 결정·검증된 출처로 바꾸거나, 해당 없음인 이유를 적기 전에는
> 승인된 PRD로 표시하지 않는다. 모르는 값을 추측해 채우지 않는다.

## 문서 위치

- 저장 경로: `docs/metis/<goal-slug>/prd.md` — `goal prd`의 반환 경로를 사용
- 같은 목표의 파생 문서: `plan.md`, `decisions.md` (각 durable 근거가 생긴 뒤 저장)
- 재개 시: 현재 run의 `goal restore.documents`를 사용; 제목으로 폴더 재생성 금지

## 원래 요청

> [사용자가 처음 표현한 목표를 가능한 한 그대로 붙여 넣는다. 요약으로
> 대체하지 않는다.]

- 요청 출처: [사용자 대화 / 출처 식별자]
- 해석 메모(선택): [원문과 구분되는 해석. 없으면 `없음`]

## 목표와 성공 결과

- 목표: [누구에게 어떤 변화가 필요한가]
- 제품 결과: [사용자·업무·제품에 관찰될 변화]
- 제품 결과 지표: [지표 / 현재값 / 목표값 / 측정 시점. 모르면 `미정`]

## 범위

### 포함

- [이번 목표에 포함되는 대상 또는 결과]

### 비범위

- [이번 목표에서 해결하지 않는 인접 문제와 필요한 경우 이유]

## 요구사항과 수용조건

독립적으로 검증 가능한 요구마다 한 행을 사용한다. 요구사항을 합쳐 쓰지
않으며, ID는 문서가 갱신되어도 유지한다.

| ID | 우선순위 | 요구사항 | 수용조건 | 관련 근거 |
| --- | --- | --- | --- | --- |
| `REQ-[영역]-001` | `must`/`should`/`could` | [필요한 결과] | [관찰 가능한 조건, Given/When/Then 또는 측정 문장] | [FACT/DECISION/ASSUMPTION/QUESTION ID] |

## Engineering 완료조건

제품 결과 지표와 별도로, 구현·검증·문서화가 계약에 맞음을 판정하는 조건만
적는다. 구현 단계가 아닌 지금 확인할 수 없는 항목은 `미정`으로 둔다.

- [ ] [요구 ID별 검증 증거의 형태]
- [ ] [필요한 테스트·검사·검토]
- [ ] [필요한 문서·운영 변경]
- [ ] [해당 없음 또는 미정인 이유]

## 제약과 위험

- 제약: [권한, 호환성, 보안·개인정보, 성능, 일정, 변경 경로 등. 없으면
  `미정`]
- 위험: [발생 가능성과 영향. 모르면 `미정`]
- 사용자 승인 필요: [예/아니오/미정 — 무엇을 승인해야 하는가]

## 사실 (FACT)

| ID | 확인된 사실 | 출처 (`source` / `path` / `hash` / `observed`) |
| --- | --- | --- |
| `FACT-001` | [검증된 사실만] | [값 또는 `미정`] |

## 사용자 결정 (USER DECISION)

| ID | 결정 | 결정자/시점 | 영향받는 요구·범위 |
| --- | --- | --- | --- |
| `DEC-001` | [사용자가 명시적으로 선택·승인한 내용] | [사용자 / 시점 또는 `미정`] | [ID] |

## 가정 (ASSUMPTION)

| ID | 가정 | 검증 방법 또는 만료 조건 | 영향 |
| --- | --- | --- | --- |
| `ASM-001` | [검증되지 않은 임시 전제] | [어떻게 확인할지 / 언제 폐기할지] | [범위·수용조건 등] |

## 미정 질문

| ID | 질문 | 답변 주체 | 영향 | 상태 |
| --- | --- | --- | --- | --- |
| `Q-001` | [승인 전에 답해야 하거나 명시적으로 수용할 질문] | [사용자/담당자/미정] | [범위·지표·수용조건 등] | `미정` |

## 다음 단계와 경계

- PRD 상태: `초안` / `사용자 검토 중` / `승인됨` / `보류` 중 하나만 선택
- 승인 조건: [사용자가 확인할 항목. 미정이면 `미정`]
- 승인 후: 공통 진입점의 기존 Metis 절차에 따라 필요한 subagent 단계로 위임
- 이 문서에서 수행하지 않음: 구현, 테스트 실행, plan 봉인, `start`/`next`/
  `drive`/`spawn` 루프

## 출처 메모 (선택)

외부 또는 저장소 밖 자료를 사용했다면 필요한 사실만 요약하고 아래 메타데이터를
남긴다. credentials, 개인 경로, private 대화 전체 내용, 외부 `PRD_Bot.md`
원본 자체는 복사하지 않는다.

- `source`: [식별자]
- `path`: [저장소 상대 경로 또는 공개 경로]
- `hash`: [콘텐츠 hash 또는 `미정`]
- `observed`: [버전·시점 또는 `미정`]
